---
name: "blogger-distiller"
description: "蒸馏抖音单条爆款视频：解析视频分享链接，用豆包视觉模型直传拆解为带时间戳的信息档案，再蒸馏出该视频的打法要点与可直接照做的行动包。当用户提供单条视频分享链接（v.douyin.com/xxx 或 douyin.com/video/<id>），要求分析学习该视频的打法时调用。"
---

# 博主蒸馏器（blogger-distiller）· 单视频链接模式

把"看一条视频学东西"变成一条可重复的流水线：**拆片 → 蒸馏 → 行动包**。输入单条视频分享链接，产出该视频的《蒸馏说明》——不是抽象分析，而是用户能直接照着拍的行动指导。

> 本技能仅支持**单条视频分享链接**。不支持博主主页批量、不支持按名字搜索、不支持图文作品链接。

## 技能结构

```
blogger-distiller/
├── SKILL.md                        # 本文件：编排层（流程总控 + 蒸馏方法论）
├── scripts/
│   ├── download/
│   │   └── dy_dl.py                # 单视频下载（直传失败时的本地回退，CDP 拦截 + 自动校验）
│   └── extract/
│       ├── dy_extract.py           # ★ 入口：单视频一键提取（默认免下载直传，失败自动回退下载上传）
│       ├── extract_video_info.py    # 火山方舟多模态提取
│       └── prompt.txt              # "只提取、不分析"提示词
└── references/
    ├── download-playbook.md        # 收片手册：单一视频版（直传/下载+校验+进程回收）
    └── extract-playbook.md         # 拆片手册 ★ 执行拆片前必读
```

## 主要流程（单视频）

| 步骤 | 工具 | 职责 | 产物 |
|---|---|---|---|
| 拆片 | scripts/extract/dy_extract.py | 解析链接→直传豆包提取 10 类信息（带时间戳） | 该视频的信息档案 .md |
| 蒸馏 | 无脚本，纯分析 | 通读档案，提炼打法要点 + 转化为行动方案 | 蒸馏说明（随对话交付） |

关键认知：拆片层的提示词被约束为"只提取、不分析"——**蒸馏层的职责才是分析**。拆片只产出事实档案，不打爆款结论。

## 环境依赖

- Python 3.10+；`pip3 install --user websockets requests`
- ffmpeg 在 PATH 中；macOS 需 Google Chrome
- `ARK_API_KEY` 环境变量（缺失时向用户索要，只进环境变量，绝不写文件）

## 工作流程

### 第 1 步：确认输入并解析

1. 用户提供单条视频分享链接（`v.douyin.com/xxx` 短链，或 `douyin.com/video/<id>` 长链）。
2. 确认目标：只讲打法、还是也要与用户自己的领域做映射（可用 AskUserQuestion，若不明确则默认"提炼打法 + 行动包"）。

### 第 2 步：拆片（核心执行）

**先读 [references/extract-playbook.md](references/extract-playbook.md)**，然后跑入口脚本：

```bash
cd <技能目录>/scripts/extract
export ARK_API_KEY=<key>
python3 dy_extract.py "<视频分享链接>" -o <运行目录>/extracts/<视频名>/
```

- `dy_extract.py` 默认**免下载直传**豆包：headless Chrome（独立 profile，无登录态）拿合并 MP4 直链（≤50MB 且匿名可访问时），直传成功即可在本地不落盘视频。直传任一环节失败自动回退「下载后上传」（用仓库内 `scripts/download/dy_dl.py`）。
- 开跑前告知耗时预估（约 4 分钟/条）；用非阻塞命令 + 轮询进度。
- 单条截断时按手册用 `--split` + `--file-id` 重跑。
- 记录档案 .md 路径供下一步蒸馏读取。

### 第 3 步：蒸馏（本技能的核心价值）

通读该视频的信息档案，产出两部分：

#### A. 打法要点（该视频能打的根本原因）

每条结论都要有证据锚点（引用档案章节/时间戳），分节提炼：

- **定位/人设**：他是谁 × 给谁看 —— 从口播主题、字幕用词、出镜形象归纳（卷一 / 卷二 / 卷五）
- **选题与钩子**：这"事"为什么吸引人（卷七段落切分）、开头 0–8 秒怎么勾人（卷二转写 + 卷三画面文字）——提炼可复用的钩子句式
- **结构节奏**：段落怎么排布、镜头/跳剪节奏（卷四、卷七）、结尾收法（卷九）
- **变现信号**：带货 / 课程 / 接单 / 广告 / 私域——证据来自卷六（产品品牌）、卷九（CTA）、口播转化话术
- **增长设计**：发布时机、蹭热点模式、互动引导方式

#### B. 行动包（最终交付物）

1. **脚本骨架模板**：可复用的开头钩子（1–2 种句式，附改编例句）→ 内容主体结构 → 收尾，填空即可用
2. **可借鉴清单**：该视频可直接借鉴的元素（选题/钩子/结构/人设细节）
3. **翻拍 checklist**：若用户要照做，拍摄前的自查项（时长、节奏、字幕、钩子位置等）
4. **差距清单**：对照这支视频的制作水准，列出用户还需补的能力/工具/素材

### 第 4 步：交付

以蒸馏说明为主线向用户讲解，重点讲透行动包部分。

## 目录约定

每次运行落在一个独立目录（`<项目根>/distilled_<日期>/` 或用户指定），档案路径：

```
<run_dir>/extracts/<视频名>/
└── <YYYYMMDD_HHMM>.md       # 该视频的信息档案（按创建时间精确到分钟命名）
```

## 方法论约束（写作纪律）

1. **证据锚点强制**：每条结论须标注来源——引用档案的章节/时间戳。禁止无出处断言。
2. **三档表述**：【事实】（档案直接支持）、【推断】（由本视频归纳）、【建议】（行动包里的主观方案）——不得混写。
3. **单样本自觉**：单视频是小样本，只做定性提炼，不输出"普遍规律/爆款率"类定量结论。
4. **不编造**：没看到的画面/听不清的口播不脑补；上游档案缺节就标"该维度证据不足"。
5. **模拟/虚假数据零容忍**：任何数字必须能溯源到真实 API 返回或真实文件。

## 失败处理

| 症状 | 处理 |
|---|---|
| ARK_API_KEY 未设置 | 向用户索要，写入当前 shell 环境变量后继续，不落盘 |
| 直传超时/失败 | dy_extract.py 已自动回退下载上传；仍失败则重跑一次 |
| 下载只有音轨 | dy_dl.py 内置 ffprobe 校验 + 重试；仍失败走「登录态 detail API」直链（见 download-playbook） |
| 连不上 Chrome / 窗口弹不出 | 90% 是本工具残留的 headless Chrome 卡死：先 `pkill -f "user-data-dir=/tmp/dy-profile"` + 释放 9222，再重连/重启 Chrome；脚本已内置 try/finally 自动回收 |

## 安全红线（全局）

- 下载绝不携带用户登录态 cookie（headless 用独立 profile）；cookie 只留在用户 Chrome 里做只读查询
- 禁止在用户浏览器执行任何点赞/关注/私信等写操作
- API Key 只进环境变量，绝不写入任何文件
- 禁止伪造任何数据；缺数据明说或换接口